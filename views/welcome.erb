
<!DOCTYPE html>

<html>

<head>

	<meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <meta name="description" content="">
    <meta name="author" content="Clara Nice and a little bit of Calvin Wang">
    <link rel="icon" href="../../favicon.ico">

</head>

<body>

	<div class="container">

      <div class="jumbotron" id="sign-up">
        <h1>Welcome to BlueJade! </h1>
        <p class="lead">
          We are currently working on the beautification of this gorgeous site.<br>
          <small>By Zach Hogan, Clara Nice, Luka Milekic, Calvin Wang.</small>
        </p>
      </div>

      <div class="row marketing">
        <div class="col-md-6">
          <h2 class="text-muted">Public Feed</h2>
          <div class="panel panel-primary">
            <div class="panel-body" style="max-height: 500px; overflow-y: scroll;">
              <% count = 0 %>
              <% @publicFeed.each do |t| %>

              <div class="media">
                <div class="media-left">
                  <a href="#">
                    <img class="media-object" src='/pngs/<%= count %>.png' height="50">
                  </a>
                </div>
                <div class="media-body">
                  <h4 class="media-heading">
                    <% user = t['owner'] %>
                    <a href="/user/<%= user %>"><%= user %></a>
                    <small><%= DateTime.parse(t['created_at']).strftime("%b %e, %l:%M%P") %></small>
                  </h4>
                  <%= t['text'] %>
                </div>
              </div>
              <% count = (count + 1)%100 %>
              <% end %>

            </div>
          </div>
        </div>

        <div class="col-md-6">
          <h2 class="text-muted">Sign in <small>Welcome back, cutie :)</small></h2>
          <form action="/login" method="POST">
            <div class="form-group">
              <input type="text" name="login" placeholder="Username or email" class="form-control">
            </div>
            <div class="form-group">
              <input type="password" name="password" placeholder="Password" class="form-control">
            </div>
            <p> <button type="submit" class="btn btn-lg btn-primary">Log in</button> </p>
          </form>

          <h2 class="text-muted">New here? <small>Registering is easy!</small></h2>
          <form action="/user/register" method="POST" >
            <div class="form-group">
              <input type="text" name="username" placeholder="Username" class="form-control">
            </div>
            <div class="form-group">
              <input type="text" name="email" placeholder="Email" class="form-control">
            </div>
            <div class="form-group">
              <input type="password" name="password" placeholder="Password" class="form-control">
            </div>
            <p> <button class="btn btn-lg btn-primary" type="submit" value="sign up!">Sign up</button></p>
          </form>
        </div>

      </div>

    </div>
</body>

</html>
